[source]

[source.mirror]
directory = "/usr/share/cargo/registry/"

# WORKAROUND: Temporarily allow downloading from crates.io for packages not yet in Debian
# Specifically needed for: systemd-unit-edit, desktop-edit
# TODO: Re-enable this restriction once these crates are packaged in Debian unstable:
# [source.crates-io]
# replace-with = "mirror"
